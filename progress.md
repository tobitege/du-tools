# Progress

- Analyse gestartet: `live_lua_coding`, `DuMcpBridge`, `ModUiExtractor` und `D:\github\yfs-tools\lua`
- Persistenzkette identifiziert: Restore wirkt korrekt, verdächtig ist der Laufzeitpfad `setOutput` -> `getScriptOutput` -> Validierung -> `databank`
- Chat-basierte Diagnostik in `live_lua_coding/unit-onStart.lua` und `live_lua_coding/unit-onTimer-UPD.lua` eingebaut, um Send/Receive/Persist/Restore im Lua-Chat sichtbar zu machen
- Eingebetteten `SCREEN_LAYOUT_EDITOR_SOURCE` wieder unter die 50000-Zeichen-Grenze gebracht (`49742` Zeichen) durch Kürzen der Diagnostik und Entfernen redundanter `movable/resizable = true`-Flags im Default-Dokument
- Live-Vergleich gemacht: `unit-onTimer-UPD.lua` im Repo und die aktive IDE-Sync-`snippet.lua` unterschieden sich deutlich; der Live-Stand war älter als das Repo.
- Kompakte Serialisierung für Dokument und Output-Envelope in `ScreenLayoutEditor.lua` und im eingebetteten `SCREEN_LAYOUT_EDITOR_SOURCE` umgesetzt.
- Lokale Verifikation: Envelope von `4409` auf `3196` Bytes reduziert, Roundtrip mit Lua-5.1-`load`-Shim erfolgreich, `buildPersistenceRecordFromOutput(..., 50000)` liefert wieder `fits=true`.
- Live-Push für `unit/onTimer(UPD)` erfolgreich durchgeführt und per diff gegen `snippet.lua` bestätigt.
- Nach Nutzerrückmeldung "`editor sichtbar, nichts ausgewählt`" den sichtbaren `lua_editor` sauber auf `unit/onStart()` gesetzt, `live_lua_coding/unit-onStart.lua` gepusht und gespeichert.
- Nachkontrolle: `snippet.lua` entspricht jetzt `live_lua_coding/unit-onStart.lua`; `snippet.sync.json` ist dagegen noch ein altes Export-Metadatum und für diesen Push nicht aussagekräftig.
- Nach dem nächsten Live-Befund (`op` im HUD, Parse-Warnung `expected near ':'` im Chat) Ursache weiter präzisiert: `setOutput(...)` akzeptiert den großen Persistenz-Output offenbar nur als JSON-kompatiblen Payload.
- Daraufhin Screen-Envelope auf JSON umgestellt, JSON-Support für die Board-Seite außerhalb des eingebetteten Screen-Sources installiert und `unit/onTimer-UPD.lua` so angepasst, dass reine Probe-JSONs nicht mehr als Persistenzfehler gewarnt werden.
- JSON-Variante erneut live in `unit/onStart()` und `unit/onTimer(UPD)` gepusht und gespeichert.
- Neues HUD-Editor-Arbeitspaket gestartet: Pflichtdokumente fuer den aktuellen Lua-Painter-/Programming-Board-Workflow gelesen (`live_lua_coding/README.md`, `live-coding-prompt.md`, `hud_editor_v1/README.md`, `DuMcpBridge/README.md`, `bridge-live-test.md`, `ModUiExtractor/README.md`, `Renderscript.md`).
- HUD-Editor-Codepfade geprueft: `HudEditorBoard.lua`, `HudEditorBoard-onStart.lua`, `080-bridge-commands.js`, `085-ide-export.js`, `090-databank-sync.js`, `000-core.js`, `010-start-screen.js`, `020-editor-shell.js`, `screen/HudEditorScreen.lua`.
- Aktuelle Ursache fuer "verlinkter Screen rendert nichts" eingegrenzt:
  - Board setzt zwar Screen-Lua, aber nur einen Board-Wrapper statt echter RenderScript-Ausgabe.
  - Screen-Export im Editor benutzt ebenfalls noch keine echte RenderScript-Generierung.
- RenderScript-Generator umgesetzt:
  - `HudEditorBoard.lua` erzeugt jetzt standalone top-level DU-RenderScript aus dem geladenen Dokument und prueft die 50000-Zeichen-Grenze.
  - `085-ide-export.js` erzeugt jetzt denselben Stil von top-level RenderScript fuer `Export Screen` statt des alten `onDraw`-Wrappers.
  - Web-Test erweitert, damit die Screen-Generierung auf `createLayer()`/top-level Lua abgesichert bleibt.
- Lokale Verifikation abgeschlossen:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1` erfolgreich.
  - `pnpm test` in `live_lua_coding/examples/hud_editor_v1/web` erfolgreich (`7 passed`).
- Live-Verifikation teilweise abgeschlossen:
  - Pflicht-Checks und Kalibrierbild abgearbeitet.
  - `library/onStart()` live aktualisiert und gespeichert.
  - Routingfehler fuer `unit/onStart()` auf stale `snippet.sync.json` / `ide_import`-Metadaten eingegrenzt.
  - Trotz Re-Init-Versuch bleibt der verlinkte Screen im aktuellen Live-Setup schwarz; die Restursache liegt jetzt im verbleibenden Live-Runtime-/Targeting-Verhalten, nicht mehr im fehlenden lokalen Codegenerator.
- Toolbar-/Planungsschritt fuer den HUD-Editor umgesetzt:
  - Primitive-Auswahl in `020-editor-shell.js` auf ein auto-schliessendes `Shapes`-Dropdown umgebaut.
  - Dropdown-Styling in `js/assets/hud-editor.css` ergaenzt.
  - Browser-Tests in `web/tests/hud-editor.spec.js` fuer das Dropdown erweitert und an den Harness-Overlay angepasst.
  - Getrackten Snippet-Plan unter `live_lua_coding/examples/hud_editor_v1/render-shape-snippet-plan.md` angelegt.
- Lokale Verifikation fuer diesen Schritt abgeschlossen:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1` erfolgreich.
  - `pnpm test` in `live_lua_coding/examples/hud_editor_v1/web` erfolgreich (`8 passed`).
  - Keine neue DU-Live-Mutation in diesem Schritt, weil die Aenderung Toolbar-UX und Planungsartefakte betrifft, nicht direkt das Screen-Runtime-Verhalten.
- Live-Verifikation fuer den Toolbar-Schritt nachgezogen:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/publish.ps1` erfolgreich; neues Ingame-Modul `hud-editor-probe.ingame.js` publiziert.
  - Session gemaess `du-tests.md` ueber `du_list_active_sessions` auf `playerId=10000` bestaetigt und editor-sensitive MCP-Aufrufe sequenziell gehalten.
  - Zuerst visueller Inworld-Check: Demo-Screen sichtbar, Programming Board zentriert.
  - `du_open_editor_native` oeffnete den `lua_editor`, Runtime-Modul `hud-editor` wurde ueber den Probe-Checkbox-Pfad aktiviert.
  - HUD Editor live geoeffnet, `New Script` gestartet, `Shapes`-Dropdown im echten DU-Client geprueft.
  - Live-Befund: Dropdown vorhanden, 5 Primitive gelistet, oeffnet/schliesst korrekt, Trigger uebernimmt das zuletzt gewaehlte Werkzeug.
  - Cleanup abgeschlossen: HUD wieder `OFF`, Lua-Editor per `Escape` geschlossen, `du_ui_describe` meldet `visible=false`.
- Naechster Planschritt umgesetzt:
  - Neues gemeinsames Modul `js/modules/082-shape-snippets.js` angelegt und in Web-/Ingame-Manifest aufgenommen.
  - Browser-Harness um `loadSnippet(id)` erweitert.
  - Browser-Tests fuer Katalogfamilien und dokumentbasiertes Snippet-Laden hinzugefuegt.
- Lokale Verifikation fuer den Snippet-Katalog:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1` erfolgreich (`13 modules` im Web-Bundle).
  - `pnpm test` in `live_lua_coding/examples/hud_editor_v1/web` erfolgreich (`10 passed`).
- Live-Verifikation fuer den Snippet-Katalog:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/publish.ps1` erfolgreich (`14 modules` im Ingame-Bundle).
  - Initial war die live geladene Runtime-Modul-Registry noch stale; der neue Katalog fehlte trotz aktueller Dateien.
  - Nach sauberem `Escape`-Close des Editors plus `du_reinject_lua_probe` war `window.HudEditor.shapeSnippets` live vorhanden.
  - `shapeSnippets.loadDocument('overlap_same_type_boxes')` lud im echten DU-Client ein 3-Elemente-Dokument; der sichtbare Canvas zeigte die ueberlappenden Boxen korrekt.
  - Cleanup erneut abgeschlossen: HUD `OFF`, Lua-Editor geschlossen, Probe meldet `visible=false`.
- Gemeinsame Screen-Command-Schicht umgesetzt:
  - Neues Modul `js/modules/083-screen-commands.js` angelegt und in Web-/Ingame-Manifest aufgenommen.
  - `085-ide-export.js` auf `buildScreenCommandDocument(...)` und `doc.c` umgestellt.
  - `board/HudEditorBoard.lua` und `screen/HudEditorScreen.lua` auf dieselbe kompakte Befehlsstruktur (`shape`, `line`, `text`) umgestellt.
  - `README.md`, `render-shape-snippet-plan.md` und die Browser-Tests auf den neuen Vertrag aktualisiert.
- Lokale Verifikation fuer die Command-Schicht:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1` erfolgreich (`14 modules` im Web-Bundle).
  - `pnpm test` in `live_lua_coding/examples/hud_editor_v1/web` erfolgreich (`11 passed`).
- Live-Verifikation fuer die Command-Schicht:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/publish.ps1` erfolgreich (`15 modules` im Ingame-Bundle).
  - Nach `du_reinject_lua_probe` meldete der echte Lua-Editor live `HudEditor.shapeSnippets`, `HudEditor.screenCommands` und `HudEditor.ideExport`.
  - `shapeSnippets.loadDocument('overlap_same_type_boxes')` ergab live `elementCount=3` und `commandCount=3`; `effect_text_rgb_split` ergab live nur `text`-Befehle.
  - `ideExport.buildScreenCode(...)` lief live auf der neuen `doc.c`-Struktur, und `ideExport.exportScreen()` lieferte erfolgreich ein `requestId`.
  - Cleanup wieder abgeschlossen: HUD `OFF`, Lua-Editor geschlossen, zentrierter Inworld-Capture wiederhergestellt.
- Neues Doku-/Recovery-Paket umgesetzt:
  - `live_lua_coding/examples/hud_editor_v1/e2e_testing.md` neu angelegt und den kompletten Start-bis-Screen-Validierungsablauf dokumentiert.
  - `du-tests.md` um den bevorzugten Recovery-Pfad `du_ui_invoke(uiKind = lua_editor, method = close_runtime_ui)` erweitert.
  - `ModUiExtractor/payload/lua-editor-runtime-modules/README.md` um den optionalen Runtime-Modul-Hook `closeUi(reason)` erweitert.
- Code fuer generisches Runtime-UI-Schliessen umgesetzt:
  - `js/modules/000-core.js` exponiert jetzt `HudEditor.closeUi(...)`.
  - `scripts/build.ps1` erzeugt Runtime-Modul-Wrapper mit `closeUi(...)`.
  - `070-runtime-module-manager.js` sammelt und ruft optionale `closeUi`/`closeUI`/`dismissUi`-Hooks aktiver Runtime-Module.
  - `035-lua-mcp-runtime.js` unterstuetzt jetzt den Probe-Methodennamen `close_runtime_ui`.
  - `DuMcpBridge/src/tools/editorTools.ts` akzeptiert `close_runtime_ui` im generischen `du_ui_invoke`-Pfad.
- Lokale Verifikation fuer dieses Paket abgeschlossen:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1` erfolgreich.
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1 -Target ingame` erfolgreich.
  - `pwsh -File ModUiExtractor/tools/build-lua-probe.ps1` erfolgreich.
  - `pnpm test` in `live_lua_coding/examples/hud_editor_v1/web` erfolgreich (`11 passed`).
  - `pnpm build` in `DuMcpBridge` erfolgreich.
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/publish.ps1` erfolgreich.
- Live-Status fuer die neue Recovery-Methode:
  - Sessionliste zeigte weiter `playerId=10000`.
  - Der anschliessende echte DU-Capture zeigte jedoch den `Dual Universe Account`-Login-Screen.
  - Daher wurde die neue `close_runtime_ui`-Methode in diesem Durchlauf nicht als erfolgreich live validiert markiert.
- Neues `shapes.lua`-Arbeitspaket umgesetzt:
  - HUD-Editor-Dokumentmodell und UI um `bezierArc`, `triangle`, `quad`, `image`, `rotation`, `shadowBlur` und `shadowColor` erweitert.
  - Preview/Properties/Tooling/Bridge-Normalisierung auf die neuen Felder gebracht.
  - `082-shape-snippets.js` um `demo_shapes_lua_full` erweitert; das Demo materialisiert den kompletten `shapes.lua`-Aufbau als 63-Elemente-Dokument.
  - `083-screen-commands.js`, `085-ide-export.js`, `screen/HudEditorScreen.lua` und `board/HudEditorBoard.lua` auf den erweiterten Command-Vertrag gebracht.
  - README und Browser-Tests fuer das neue Shape-Menue und die Voll-Demo aktualisiert.
- Lokale Verifikation fuer den `shapes.lua`-Ausbau abgeschlossen:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1` erfolgreich.
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/build.ps1 -Target ingame` erfolgreich.
  - `pnpm test` in `live_lua_coding/examples/hud_editor_v1/web` erfolgreich (`13 passed`).
- Live-Verifikation fuer den Editor-/Persistenzpfad:
  - `pwsh -File live_lua_coding/examples/hud_editor_v1/scripts/publish.ps1` erfolgreich.
  - Session gemaess `du-tests.md` erneut ueber `du_list_active_sessions` auf `playerId=10000` bestaetigt.
  - Echten `lua_editor` geoeffnet, wegen frischem Runtime-Modul zuerst sauber geschlossen und anschliessend per `du_reinject_lua_probe` neu injiziert.
  - Per `raw_eval` bestaetigt: `window.HudEditor.shapeSnippets` live vorhanden, `demo_shapes_lua_full` live vorhanden.
  - `shapeSnippets.loadDocument('demo_shapes_lua_full')` im echten HUD Editor geladen; sichtbarer DU-Screenshot zeigte das neue editierbare Layout live im Overlay.
  - Live-Exportpfad bestaetigt: `buildScreenCode(...)` enthielt `addBezier`, `addTriangle`, `addQuad`, `addImage`, `setNextRotation`, `setNextShadow`.
  - Save ueber den echten HUD-Editor ausgelost; anschliessend `unit/onStart()` sichtbar verifiziert und bestaetigt, dass das Buffer den neuen Demo-Dokumentkopf und die neuen Felder enthaelt.
- Live-Verifikation fuer den Board-/Screen-Runtimepfad:
  - Erster echter Board-Off/On nach dem Save brachte frische Lua-Chatzeilen fuer den groesseren Dokument-Input (`ic=8190`), der sichtbare Screen zeigte aber nur ein falsches Teilbild.
  - Ursache eingegrenzt: `library/onStart()` auf dem Construct lief noch mit einer aelteren `HudEditorBoard.lua`-Version.
  - Danach `live_lua_coding/examples/hud_editor_v1/board/HudEditorBoard.lua` live nach `library/onStart()` gepusht und gespeichert.
  - Zweiter echter Board-Off/On brachte frische Lua-Chatzeilen fuer den neuen groesseren Publish (`sc=5820`, `ic=9019`) auf `Modern Screen m [38]`.
  - Der sichtbare Screen blieb danach schwarz. Nach `du-tests.md` ist das der massgebliche Live-Befund; der Restfehler liegt jetzt im Live-Screen-Runtimeverhalten.
